---
import { siteContent } from "../data/siteContent";

const serviceLinks = siteContent.home.one.services;
const companyLinks = [
  ["Arbeiten", "/#arbeiten"],
  ["Über uns", "/agentur"],
  ["Preise", "/preise"],
  ["Kontakt", "/kontakt"],
  ["Impressum", "/impressum"],
  ["Datenschutz", "/datenschutz"],
] as const;
---

<footer class="border-t border-[var(--dw-card-border)] bg-white px-5 py-12 text-[var(--dw-ink)]">
  <div class="mx-auto grid max-w-[1180px] gap-10 lg:grid-cols-[0.9fr_1.1fr]">
    <div>
      <a href="/" class="inline-flex min-h-11 items-center gap-2 rounded-full outline-none transition hover:translate-x-0.5">
        <span class="grid h-10 w-10 place-items-center rounded-full bg-[var(--dw-dark)] font-impact text-sm font-black text-white">D</span>
        <span class="font-impact text-lg font-black">{siteContent.brand.name}</span>
      </a>
      <p class="mt-5 max-w-md text-sm font-medium leading-7 text-[var(--dw-ink-soft)]">
        Websites, SEO und Kampagnen für Unternehmen, die online professioneller wirken und mehr passende Anfragen gewinnen wollen.
      </p>
      <a class="mt-5 inline-flex min-h-11 items-center text-sm font-black text-[var(--dw-accent-strong)]" href={`mailto:${siteContent.brand.email}`}>
        {siteContent.brand.email}
      </a>
    </div>
    <nav class="grid gap-8 sm:grid-cols-2" aria-label="Footer">
      <div>
        <p class="font-impact text-lg font-black">Leistungen</p>
        <div class="mt-4 grid gap-3">
          {serviceLinks.map((link) => <a href={link.href} class="inline-flex min-h-11 items-center text-sm font-bold text-[var(--dw-ink-soft)] transition hover:text-[var(--dw-accent-strong)]">{link.title}</a>)}
        </div>
      </div>
      <div>
        <p class="font-impact text-lg font-black">Digitalwerk</p>
        <div class="mt-4 grid gap-3">
          {companyLinks.map(([label, href]) => <a href={href} class="inline-flex min-h-11 items-center text-sm font-bold text-[var(--dw-ink-soft)] transition hover:text-[var(--dw-accent-strong)]">{label}</a>)}
        </div>
      </div>
    </nav>
  </div>
  <div class="mx-auto mt-10 flex max-w-[1180px] flex-col gap-2 border-t border-[var(--dw-card-border)] pt-5 text-xs font-bold text-[var(--dw-muted)] sm:flex-row sm:items-center sm:justify-between">
    <p>{siteContent.brand.location} · {siteContent.brand.addressLine}</p>
    <p>© {new Date().getFullYear()} {siteContent.brand.name}</p>
  </div>
</footer>
